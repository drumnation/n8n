import type {Config} from 'prettier';

const config: Config = {
  tabWidth: 2,
  useTabs: false,
  semi: true,
  printWidth: 80,
  singleQuote: true,
  arrowParens: 'always',
  endOfLine: 'auto',
  bracketSpacing: false,
  bracketSameLine: false,
};

export default config;
